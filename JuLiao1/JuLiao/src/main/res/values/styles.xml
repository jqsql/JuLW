<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    <style name="Theme.FloatActivity" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        是否浮现在activity之上
        <item name="android:windowIsFloating">true</item>
        半透明
        <item name="android:windowIsTranslucent">false</item>
        无标题
        <item name="android:windowNoTitle">true</item>
        背景透明
        <item name="android:windowBackground">@android:color/transparent</item>
        模糊
        <item name="android:backgroundDimEnabled">false</item>
        除去背景色
    </style>


    -->

    <style name="payMoney_tvback_style01">
        <item name="android:background">@drawable/cornerfulllinefull_greenline_whitefull</item>
        <item name="android:padding">6dp</item>
        <item name="android:paddingTop">6dp</item>
    </style>

    <style name="payMoney_tvback_style02">
        <item name="android:background">@drawable/shape_trap_4</item>
        <item name="android:padding">12dp</item>
    </style>
 <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!-- API 11 theme customizations can go here. -->
        <item name="android:windowBackground">@drawable/bg_gradual_change</item>
    </style>

</resources>